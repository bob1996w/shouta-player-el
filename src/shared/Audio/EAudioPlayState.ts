export enum EAudioPlayState {
    // the audio is playing.
    Playing = 'Playing',
    // the audio is paused.
    Paused = 'Paused',
    // the audio is stopped / no audio has been initialized.
    Stopped = 'Stopped'
}